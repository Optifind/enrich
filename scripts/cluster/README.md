# Cluster Products

Cluster is a script used to cluster products by style and use case. The script expects catalog to be already initialized and saved to database. Clustering options can be configured in `data/config.json`.

Run the script by running this in the project root:

```bash
make cluster
``` 
