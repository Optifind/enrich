# `deploy/`

`deploy/` is used for storing code and scripts related to deploying and running the app. This includes things like Docker Compose files etc.

In our case `deploy/` doesn't contain anything that deals with building or pushing app images to Docker Hub. This can be found under `build/` and `Makefile`.
